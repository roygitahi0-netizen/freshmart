# Base URL of your Flask backend (no trailing slash, no /api — that's
# appended automatically). e.g. http://localhost:5000 in development,
# https://api.freshminimart.co.ke in production.
# Leave empty to run the frontend in preview mode with local sample data.
VITE_API_BASE_URL=http://localhost:5000
